{
  "extension": ["ts"],
  "forbid-only": true,
  "recursive": true,
  "reporter": "spec",
  "require": "ts-node/register",
  "spec": "test/**/*.test.ts",
  "timeout": 10000,
  "watch-files": ["test/**/*.test.ts"]
}
